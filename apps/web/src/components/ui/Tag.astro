---
import styles from './Tag.module.css'

interface Props {
  variant?: 'default' | 'category'
  size?: 'sm' | 'md'
  href?: string
  class?: string
}

const {
  variant = 'default',
  size = 'md',
  href,
  class: className = '',
  ...rest
} = Astro.props

const classes = [
  styles.tag,
  variant !== 'default' && styles[`tag--${variant}`],
  size !== 'md' && styles[`tag--${size}`],
  className,
].filter(Boolean).join(' ')
---

{href ? (
  <a href={href} class={classes} {...rest}>
    <slot />
  </a>
) : (
  <span class={classes} {...rest}>
    <slot />
  </span>
)}
