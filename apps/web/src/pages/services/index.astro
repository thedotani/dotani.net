---
import Layout from '../../layouts/Layout.astro'
import { getAllServices } from '../../lib/sanity'

const services = await getAllServices()
---

<Layout title="Services" description="Our services">
  <main class="section-container py-20">
      <h1 class="section-heading mb-4">Our Services</h1>
      <p class="section-tagline mb-12 max-w-2xl">Strategy, content, and digital presence services for brands and leaders.</p>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        {services?.map((service: any) => (
          <article class="card-surface p-6 md:p-7 h-full flex flex-col">
          {service.icon && (
            <div class="w-12 h-12 rounded-lg bg-primary/10 flex items-center justify-center mb-4">
              <span class="text-primary text-xl">{service.icon}</span>
            </div>
          )}
            <h2 class="font-display text-xl font-semibold text-heading mb-2">
            {service.title}
          </h2>
          {service.shortDescription && (
            <p class="text-muted mb-4">
              {service.shortDescription}
            </p>
          )}
          {service.features && service.features.length > 0 && (
            <ul class="space-y-2 mb-4">
              {service.features.slice(0, 3).map((feature: any) => (
                <li class="text-sm text-muted flex items-start">
                  <span class="text-primary mr-2">•</span>
                  {feature.text || feature}
                </li>
              ))}
            </ul>
          )}
          {service.bestFor && (
            <p class="text-sm text-accent mb-4">
              Best for: {service.bestFor}
            </p>
          )}
          <a
            href={`/services/${service.slug?.current}`}
            class="mt-2 inline-block text-sm font-medium text-primary hover:underline"
          >
            View Service →
          </a>
        </article>
      ))}
    </div>
  </main>
</Layout>